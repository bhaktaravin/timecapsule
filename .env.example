# 32-byte master key, hex-encoded (64 characters). Generate with:
# openssl rand -hex 32
MASTER_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

DATABASE_URL=sqlite:timecapsule.db?mode=rwc
HOST=127.0.0.1
PORT=3000
RUST_LOG=timecapsule=debug,tower_http=debug
DEV_MODE=1

# Email notifications (optional). When set, recipients receive an unlock link at unlock time.
# APP_BASE_URL=http://127.0.0.1:3000
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=
# SMTP_FROM=Timecapsule <noreply@example.com>
